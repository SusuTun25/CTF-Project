import React from 'react';

function ChallengeInterface({ challenge }) {
  return (
    <div className="challenge-interface">
      <h2>{challenge.title} Interface</h2>
      {/* Add your interface elements here */}
    </div>
  );
}

export default ChallengeInterface;